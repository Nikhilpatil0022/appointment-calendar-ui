import React from "react";

const VARIANTS = {
  DANGER: "danger", // overdue
  CRITICAL: "critical", //orange
  SAFE: "safe", // green (done successfully)
  NEUTRAL: "neutral", //blue (ongoing)
};

const Card = (props) => {
  return <div>Card</div>;
};

export default Card;
